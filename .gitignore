# Runtime artifacts (generated locally, never commit — may contain usage data)
usage-snapshot.json
turn-log.jsonl
rtk-cache.txt
tokenscope-daily.json
tokstats-dashboard.html
tokscope-sessions/
*.tokdelta

# Local index
.memsearch/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
*.swp

# Local-only session handover note (not for publishing)
HANDOVER.md
